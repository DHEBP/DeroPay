import { AgentSettlementsPage } from "./agent-settlements-page";

export const metadata = {
  title: "Agent payments — DeroPay",
};

export default function Page() {
  return <AgentSettlementsPage />;
}
